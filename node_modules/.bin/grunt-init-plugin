#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-init-plugin/bin/grunt-init-plugin" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-init-plugin/bin/grunt-init-plugin" "$@"
  ret=$?
fi
exit $ret
